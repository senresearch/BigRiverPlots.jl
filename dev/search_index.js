var documenterSearchIndex = {"docs":
[{"category":"section","location":"scores/#Scores-plot","page":"Scores","text":"The scores plot draws each observation as a point in the space of two components, with crosshairs at the origin and, when we ask for it, one color per class.\n\nplot_scores(scores; group = ..., comps = (1, 2), kwargs...)\n\nIt takes a scores matrix — observations in rows, components in columns — so anything that produces scores can be drawn with it: PCA, PLS, CCA, JIVE. It never sees the model itself.\n\nWe simulate a small data set, fit a PCA, look at the default plot, and then build it up.","title":"Scores plot"},{"category":"section","location":"scores/#Setup","page":"Scores","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs\n","title":"Setup"},{"category":"section","location":"scores/#A-simulated-example","page":"Scores","text":"Three latent signals drive twenty variables, plus a little noise. We read the class label off the first signal, so the observations fall into three groups that PCA should separate along the first component.\n\nThe same simulation runs through the other notebooks, so the figures are comparable across plots.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\n# the class is read off the first latent signal\ny = [latent[i, 1] > 0.4 ? \"a\" : latent[i, 1] < -0.4 ? \"c\" : \"b\" for i in 1:n]\n\nm = pca(X; k = 5)\nS = pca_transform(m, X)\n\nsize(S)   # observations by components","title":"A simulated example"},{"category":"section","location":"scores/#The-default-plot","page":"Scores","text":"Given nothing but the scores, we get the first two components, generic axis labels, and crosshairs marking zero. We passed no grouping, so every observation sits in one series and there is no legend to draw.\n\nplot_scores(S)\n\n","title":"The default plot"},{"category":"section","location":"scores/#Modifying-the-plot","page":"Scores","text":"Two arguments change what we draw — group and comps. Everything else changes how it looks: origincolor for the crosshairs, and the whole standard Plots vocabulary for the rest. Anything the plot sets for itself is a default that yields to whatever we pass.","title":"Modifying the plot"},{"category":"section","location":"scores/#Grouping-the-observations","page":"Scores","text":"group splits the observations into one series per level, which gives us categorical colors and one legend entry per class rather than the continuous colorbar a marker_z would produce. The legend appears on its own once there is more than one level, and its text comes straight from the vector we pass — so relabelling the vector relabels the legend.\n\nHere we also name the axes properly and give the figure a title.\n\nplot_scores(S;\n            group  = replace(y, \"a\" => \"low\", \"b\" => \"mid\", \"c\" => \"high\"),\n            xlabel = \"PC 1\",\n            ylabel = \"PC 2\",\n            title  = \"PCA scores by class\")","title":"Grouping the observations"},{"category":"section","location":"scores/#Choosing-components,-and-restyling","page":"Scores","text":"comps picks any other pair of components. The axis labels do not follow it, so when we move off the first two we say so ourselves.\n\nThis is also where the styling options stack up. Below we change the pair of components, redden the crosshairs with origincolor, enlarge the markers, move the legend inside the axes, and shrink the canvas — all in one call.\n\nplot_scores(S;\n            group       = y,\n            comps       = (1, 3),\n            xlabel      = \"PC 1\",\n            ylabel      = \"PC 3\",\n            title       = \"components 1 and 3\",\n            origincolor = :red,\n            marker      = 10,\n            legend      = :topleft,\n            size        = (600, 450))","title":"Choosing components, and restyling"},{"category":"section","location":"scores/#Without-a-legend,-on-a-wide-canvas","page":"Scores","text":"When the grouping is obvious from context, or when the figure is going into a paper with its own caption, we drop the legend and reshape the canvas.\n\nplot_scores(S;\n            group             = y,\n            legend            = false,\n            marker            = 4,\n            markerstrokewidth = 0,\n            size              = (900, 320), \n            palette = [:orange, :seagreen, :purple],\n            left_margin        = (5,:mm),\n            bottom_margin        = (5,:mm),\n            title             = \"no legend, wide\")","title":"Without a legend, on a wide canvas"},{"category":"section","location":"loadings/#Loadings-plot","page":"Loadings","text":"Where the scores plot shows the observations, the loadings plot shows the variables: how much each one contributes to a single component. Each variable gets a bar drawn from a line at zero, so the sign and the magnitude both read at a glance.\n\nplot_loadings(loadings; comp = 1, varnames = ..., ntop = ..., nonzero = ..., kwargs...)\n\nIt takes a loadings matrix with variables in rows and components in columns. Every model in BigRiverEssence stores one in that layout already, so nothing needs transposing:\n\nModel Call\npca plot_loadings(m.loadings; ntop = 20)\nplskern plot_loadings(m.P; ntop = 20)\nplsda plot_loadings(m.loadings_X; ntop = 20)\ncca plot_loadings(m.xproj)\nspc plot_loadings(m.loadings; nonzero = true, style = :sticks)\nsplsda plot_loadings(m.loadings_X; nonzero = true, style = :sticks)\nscca plot_loadings(m.u; nonzero = true, style = :sticks)\npmd plot_loadings(m.v; nonzero = true, style = :sticks)\njive plot_loadings(m.U[i])\n\nThe two-block models draw their Y side the same way, from m.loadings_Y, m.yproj or m.v.","title":"Loadings plot"},{"category":"section","location":"loadings/#Setup","page":"Loadings","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs","title":"Setup"},{"category":"section","location":"loadings/#A-simulated-example","page":"Loadings","text":"We use the same data as the scores notebook, so the two sets of figures describe one fit from both sides. Three latent signals drive twenty variables, plus a little noise — so we expect a handful of variables to carry most of the weight on each component and the rest to sit near zero.\n\nWe also name the variables here. Names matter more for this plot than for the scores, since the variable axis is what we read.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\n# names for the variable axis\nvnames = [\"gene$(i)\" for i in 1:p]\n\nm = pca(X; k = 5)\n\nsize(m.loadings)   # variables by components","title":"A simulated example"},{"category":"section","location":"loadings/#The-default-plot","page":"Loadings","text":"Given nothing but the loadings, we get the first component, every variable in its original order, ticked by index, with a line at zero separating the positive contributions from the negative.\n\nplot_loadings(m.loadings)\n\n","title":"The default plot"},{"category":"section","location":"loadings/#Modifying-the-plot","page":"Loadings","text":"Three arguments change which loadings we draw — comp, ntop and nonzero — and are applied before the plot is built. Everything else changes how it looks: style for bars against sticks, two color knobs, and the whole standard Plots vocabulary.","title":"Modifying the plot"},{"category":"section","location":"loadings/#Choosing-what-to-draw","page":"Loadings","text":"comp picks the component — the first is drawn by default. varnames names the variables; we pass them as an argument rather than setting xticks, because they are subset along with the loadings, so whichever variables survive keep their correct names. ntop keeps only the variables with the largest absolute loading — the ones actually driving the component — leaving them in their original order rather than sorting them.\n\nOver many variables ntop is what keeps the axis legible, and it is what we reach for with the dense models. The axis label does not follow comp, so when we move off the first component we say so ourselves.\n\nplot_loadings(m.loadings;\n              comp     = 3,\n              varnames = vnames,\n              ntop     = 8,\n              ylabel   = \"Loading on PC 3\",\n              title    = \"the eight largest contributors to PC 3\")","title":"Choosing what to draw"},{"category":"section","location":"loadings/#The-sparse-models","page":"Loadings","text":"Models with an L1 penalty (spc, pmd, splsda, scca) push most loadings to exactly zero. Drawing all of them wastes the axis on empty bars, so nonzero keeps only the variables that survived the penalty, and loadingsstyle = :sticks swaps the bars for stems — a stem reads sparsity better, putting the emphasis on the few variables present rather than on the width of each bar.\n\nOur PCA is dense, so nonzero changes nothing here and the figure below is really just showing the stick style. On a sparse fit these two together are what make the plot readable.\n\nplot_loadings(m.loadings;\n              varnames = vnames,\n              nonzero  = true,\n              loadingsstyle    = :sticks,\n              title    = \"stems, as a sparse fit would be drawn\")","title":"The sparse models"},{"category":"section","location":"loadings/#Colors-and-the-rest","page":"Loadings","text":"Two color knobs of our own: loadingscolor for the bars and origincolor for the line at zero, which defaults to a light shade so it reads as a reference rather than as data.\n\nEverything the plot sets for itself is a default that yields to whatever we pass, so the full Plots vocabulary is available alongside them — title, fonts, canvas size, legend.\n\nplot_loadings(m.loadings;\n              varnames      = vnames,\n              ntop          = 10,\n              loadingscolor = \"#d94801\",\n              origincolor   = :red,\n              ylabel        = \"Loading on PC 1\",\n              title         = \"PCA loadings, component 1\",\n              size          = (800, 450),\n              left_margin        = (5,:mm),\n              bottom_margin        = (5,:mm),\n              guidefontsize = 9,\n              legend        = false)","title":"Colors and the rest"},{"category":"section","location":"vip/#VIP-plot","page":"Vip","text":"The VIP plot ranks the variables by their Variable Importance in Projection and lays them out as horizontal bars, the largest at the top, with a dashed line at the threshold of one.\n\nplot_vip(vips; comp = 0, varnames = ..., above = false, ntop = 0, kwargs...)\n\nVIP scores are scaled so their mean square is one, which is what makes the threshold meaningful: a variable above the line contributes more than average, one below it less. Over many variables the sorted bars read as a smooth curve, and the number of important variables is simply where that curve crosses the line.\n\nThe VIP is defined for the discriminant models alone and comes from the vip function:\n\nModel Call\nplsda plot_vip(vip(m))\nsplsda plot_vip(vip(m))\n\nThe other models store no VIP. Their variable importance is read from the loadings plot instead.","title":"VIP plot"},{"category":"section","location":"vip/#Setup","page":"Vip","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs\n","title":"Setup"},{"category":"section","location":"vip/#A-simulated-example","page":"Vip","text":"This notebook uses a wider simulation than the others — two hundred variables rather than twenty — because the sorted bars only read as a curve when there are enough of them to form one. On twenty variables we would see twenty fat bars; on two hundred we see the shape that real data gives.\n\nThree latent signals drive the variables and the class is read off the first, so a handful should stand out well above the threshold while the rest fall below it.\n\nrng = StableRNG(20240801)\n\nn = 120    # observations\np = 200    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\ny = [latent[i, 1] > 0.4 ? \"a\" : latent[i, 1] < -0.4 ? \"c\" : \"b\" for i in 1:n]\n\nvnames = [\"gene$(i)\" for i in 1:p]\n\nm = plsda(X, y, 3)\nV = vip(m)\n\nsize(V)   # variables by components","title":"A simulated example"},{"category":"section","location":"vip/#The-default-plot","page":"Vip","text":"vip returns a matrix of variables by components. The scores are cumulative — column h is the importance built up through components 1 to h — so the last column is the overall importance, and that is the one drawn by default.\n\nThe variables are always sorted, largest at the top. That is not optional: the layout is a ranked curve, not a plot in variable order, and the ranking is the shape we came to read.\n\nplot_vip(V)\n\n","title":"The default plot"},{"category":"section","location":"vip/#Modifying-the-plot","page":"Vip","text":"Three things are worth separating.\n\nWhich column — comp picks a component, defaulting to 0 for the last. Because the scores are cumulative, comp = 1 gives the importance from the first component alone, which is a different and usually narrower question.\n\nHow many variables — ntop keeps a fixed count of the highest scorers, while above keeps however many clear the threshold, so the count depends on the data. A third knob, maxnames, does not filter anything — it only decides whether the variable names appear on the axis, since past a few dozen there is no room for a tick each.\n\nThe threshold line — threshold moves it, thresholdcolor colors it, thresholdline = false removes it.","title":"Modifying the plot"},{"category":"section","location":"vip/#Naming-and-trimming","page":"Vip","text":"With two hundred variables the names cannot be shown, so the axis is left bare. Cut the set down with ntop and the names appear, provided the count falls under maxnames — which defaults to thirty.\n\nHere we take the twenty most important variables and name them. thresholdline = false makes the line disappear.\n\nplot_vip(V;\n         varnames = vnames,\n         ntop     = 20,\n         xlabel   = \"VIP ($(m.ncomp) axes)\",\n         title    = \"the twenty most important genes\",\n         thresholdline = false,\n         size     = (650, 700))","title":"Naming and trimming"},{"category":"section","location":"vip/#Keeping-only-what-clears-the-line","page":"Vip","text":"above = true asks a different question: not \"the top twenty\" but \"however many are actually important\". The count is whatever the data gives, which is usually what we want when reporting a selection rather than picking a round number.\n\nRaising maxnames lets the names show even when more variables survive than the default cutoff allows.\n\nplot_vip(V;\n         varnames = vnames,\n         above    = true,\n         maxnames = 60,\n         xlabel   = \"VIP ($(m.ncomp) axes)\",\n         title    = \"every gene above the threshold\",\n         size     = (650, 800))","title":"Keeping only what clears the line"},{"category":"section","location":"vip/#The-line,-the-bars,-and-the-rest","page":"Vip","text":"The threshold sits at one by default, which is the convention. Moving it to a stricter value is a way of tightening the selection, and since above filters against whatever threshold is set to, the two work together — threshold = 1.2, above = true keeps only the variables clearing the stricter bar.\n\nvipcolor fills the bars and vipedgecolor outlines them. Everything else is a standard attribute and yields to what we pass.\n\nplot_vip(V;\n         varnames       = vnames,\n         threshold      = 1.2,\n         above          = true,\n         thresholdcolor = :blue,\n         vipcolor       = \"#6a51a3\",\n         vipedgecolor   = :grey,\n         maxnames       = 40,\n         xlabel         = \"VIP ($(m.ncomp) axes)\",\n         title          = \"a stricter threshold at 1.2\",\n         size           = (650, 700))","title":"The line, the bars, and the rest"},{"category":"section","location":"sparse/#Sparsity-plot","page":"Sparsity","text":"A model with an L1 penalty drops most variables on most components. The sparsity plot counts how many each component keeps nonzero and draws them as bars, so how hard the penalty bit — component by component — reads at a glance.\n\nplot_sparsity(loadings; comps = [...], ncomp = 0, compnames = ..., asfraction = false, kwargs...)\n\nThe models with an L1 penalty pass the field their penalty acts on:\n\nModel Call\nspc plot_sparsity(m.loadings)\npmd plot_sparsity(m.v) — the variable side, m.u for the sample side\nsplsda plot_sparsity(m.loadings_X)\nscca plot_sparsity(m.u) — the X side, m.v for the Y side\n\nThe dense models — pca, plskern, plsda, cca — select every variable on every component, so their sparsity plot would be a flat row of full bars and is not drawn.\n\nNote that pmd and scca penalize both factors, so each has a sparsity on either side. For pmd, m.v counts the variables kept and m.u the samples.","title":"Sparsity plot"},{"category":"section","location":"sparse/#Setup","page":"Sparsity","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs","title":"Setup"},{"category":"section","location":"sparse/#A-simulated-example","page":"Sparsity","text":"Forty variables driven by three latent signals, fitted with a sparse PCA. Forty is enough that the penalty leaves the counts well below the total — on twenty the bars would sit close to full and the plot would have nothing to show.\n\nWe print the counts alongside, since that column of numbers is exactly what the figure draws.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 40    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\nm = spc(X; k = 4, c = sqrt(p) / 2)\n\n# what the plot will draw, as numbers\n[count(!iszero, m.loadings[:, j]) for j in 1:4], p","title":"A simulated example"},{"category":"section","location":"sparse/#The-default-plot","page":"Sparsity","text":"One bar per component, ticked by index, with the count printed over each bar. The label is what makes this readable without squinting at the axis — the exact number matters more here than the relative height.\n\nplot_sparsity(m.loadings)\n\n","title":"The default plot"},{"category":"section","location":"sparse/#Modifying-the-plot","page":"Sparsity","text":"There are two ways to choose components and they differ in an important way. ncomp takes a leading count — ncomp = 2 gives components 1 and 2 — while comps takes an explicit vector, so a non-contiguous set like [1, 3] is possible with comps and not with ncomp.\n\nasfraction changes the quantity from a count to a proportion, compnames names the bars, labelcounts removes the numbers, and sparsitycolor sets the fill.","title":"Modifying the plot"},{"category":"section","location":"sparse/#Naming,-and-choosing-components","page":"Sparsity","text":"compnames is subset along with the counts, so the labels stay correct whatever we select — which means we pass one name per component of the matrix, not one per entry of comps. That is the same convention as varnames on the loadings plot.\n\nHere we take a non-contiguous pair, which only comps can do.\n\nplot_sparsity(m.loadings;\n              comps     = [1, 3],\n              compnames = [\"SPC $(j)\" for j in 1:4],\n              ylabel    = \"variables selected\",\n              title     = \"components 1 and 3\")","title":"Naming, and choosing components"},{"category":"section","location":"sparse/#As-a-fraction","page":"Sparsity","text":"asfraction = true divides by the total number of variables, which reads more easily when there are many — a bar at 0.15 tells us the penalty kept fifteen percent without our needing to remember what p was.\n\nThe count labels turn themselves off here, since a fraction over a bar reads worse than the axis does.\n\nplot_sparsity(m.loadings;\n              asfraction = true,\n              compnames  = [\"SPC $(j)\" for j in 1:4],\n              ylabel     = \"fraction of variables selected\",\n              title      = \"the same fit, as a proportion\",\n              ylims      = (0, 1))","title":"As a fraction"},{"category":"section","location":"sparse/#Color,-and-the-rest","page":"Sparsity","text":"sparsitycolor fills the bars and labelcounts = false removes the numbers over them — worth doing when the figure is small enough that the labels crowd, or when it sits beside others and the axis carries the reading.\n\nEverything else the plot sets for itself yields to what we pass.\n\nplot_sparsity(m.loadings;\n              compnames     = [\"SPC $(j)\" for j in 1:4],\n              sparsitycolor = \"#238b45\",\n              labelcounts   = false,\n              ylabel        = \"variables selected\",\n              title         = \"sparse PCA\",\n              size          = (600, 400),\n              guidefontsize = 10)","title":"Color, and the rest"},{"category":"section","location":"pairs/#Pairs-plot","page":"Pairs","text":"The scores plot draws one pair of components. The pairs plot draws them all: a square grid crossing every component against every other, with the scatter off the diagonal and a histogram of each component along it.\n\nplot_pairs(scores; comps = [...], group = ..., compnames = ..., kwargs...)\n\nThat is the view the scores plot cannot give. A class that separates on no single pair but does on some third one shows up here immediately, and we do not have to guess which pair to look at first.\n\nThe grid is square in the number of components crossed, so it grows fast — four make sixteen cells, six make thirty six. Three or four is usually as much as reads, and comps is there to say which.","title":"Pairs plot"},{"category":"section","location":"pairs/#Setup","page":"Pairs","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs","title":"Setup"},{"category":"section","location":"pairs/#A-simulated-example","page":"Pairs","text":"The same data as the other notebooks. We fit eight components deliberately, even though only three signals are at work — that way the grid shows us both the components carrying structure and the ones fitting noise, which is exactly the comparison the plot is for.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\n# the class is read off the first latent signal\ny = [latent[i, 1] > 0.4 ? \"a\" : latent[i, 1] < -0.4 ? \"c\" : \"b\" for i in 1:n]\n\nm = pca(X; k = 8)\nS = pca_transform(m, X)\n\nsize(S)   # observations by components","title":"A simulated example"},{"category":"section","location":"pairs/#The-default-plot","page":"Pairs","text":"With no comps every component is crossed, which here means an eight by eight grid — sixty four cells, far more than reads at the default canvas size. We keep it short instead and cross the first three.\n\nplot_pairs(S; comps = [1, 2, 3])\n\n","title":"The default plot"},{"category":"section","location":"pairs/#Modifying-the-plot","page":"Pairs","text":"comps chooses which components are crossed and group colors the points by class. compnames names them — and here is the convention worth internalising, since it is the usual way this call fails:\n\ncompnames is one name per component of the matrix, not one per entry of comps. Our matrix has eight components, so we pass eight names even when crossing three. The helper subsets them itself, so it needs the full list to index into. Writing [\"PC $(j)\" for j in 1:size(S, 2)] tracks whatever we fitted with and avoids the mismatch entirely.\n\nThe other quirk: each cell is titled by the plot, so a title we pass would repeat once per cell and is blanked. The grid takes plot_title instead.","title":"Modifying the plot"},{"category":"section","location":"pairs/#Grouping-and-naming","page":"Pairs","text":"With group the points are colored by class and one legend is written for the whole grid rather than one per cell. Each class keeps its color across every cell, so a cluster can be followed from one pair to the next.\n\nNote what the grid tells us that a single pair could not: the classes separate cleanly on the first component, and the cells crossing it with anything else all show that same split, while the cells among the later components show none.\n\nplot_pairs(S;\n           comps      = [1, 2, 3],\n           group      = y,\n           compnames  = [\"PC $(j)\" for j in 1:size(S, 2)],\n           plot_title = \"PCA scores, first three components\",\n           size       = (800, 800))","title":"Grouping and naming"},{"category":"section","location":"pairs/#A-non-contiguous-subset","page":"Pairs","text":"comps need not be consecutive. Crossing components 1, 3 and 5 is often the more useful question — whether a later component carries anything the earlier ones missed — and the diagonal histograms make the answer plain: a component fitting noise has a single unstructured mound, while one carrying signal shows the classes pulled apart.\n\nWe also bump the canvas, since the grid needs room.\n\nplot_pairs(S;\n           comps      = [1, 3, 5],\n           group      = y,\n           compnames  = [\"PC $(j)\" for j in 1:size(S, 2)],\n           plot_title = \"components 1, 3 and 5\",\n           size       = (900, 900))","title":"A non-contiguous subset"},{"category":"section","location":"pairs/#The-diagonal,-and-the-rest","page":"Pairs","text":"Two knobs of our own act on the diagonal: diagcolor sets the histogram color and diagbins the number of bins. The diagonal shares its x limits with its column so the histogram lines up with the scatter above and below it, but its y axis is a count and is left alone.\n\nEverything else the plot sets for itself yields to what we pass — marker size, canvas, fonts. Below we also pass a title, which is blanked, to show that plot_title is the one that carries.\n\nplot_pairs(S;\n           comps      = [1, 2, 3],\n           group      = y,\n           compnames  = [\"PC $(j)\" for j in 1:size(S, 2)],\n           diagcolor  = \"#d94801\",\n           diagbins   = 8,\n           markersize = 5,\n           title      = \"this will not appear\",\n           plot_title = \"restyled diagonal\",\n           size       = (850, 850))","title":"The diagonal, and the rest"},{"category":"section","location":"confidence/#Confidence-plot","page":"Confidence","text":"The confidence plot draws an estimate for each of several variables as a point with a horizontal error bar, and a red line at zero. How far a point sits from the line is the size of the effect, the bar is its confidence interval, and whether the bar crosses zero is what marks the estimate as significant — the points whose interval clears zero are coloured apart from those whose interval still contains it.\n\nplot_confidence(x, y, ε; kwargs...)\n\nIt takes three vectors directly, so it is not tied to any model: the estimates, their labels, and the half-width of each interval. Anything that produces an estimate and a spread — a regression's coefficients, a set of effect sizes, a batch of group differences — can be drawn with it.","title":"Confidence plot"},{"category":"section","location":"confidence/#Setup","page":"Confidence","text":"The confidence plot takes plain vectors rather than a fitted model, so we need only BigRiverPlots and Plots.\n\nusing BigRiverPlots\nusing Plots","title":"Setup"},{"category":"section","location":"confidence/#An-example","page":"Confidence","text":"Eight variables, each with an estimated effect and a confidence half-width. We arrange it so some intervals clear zero and some straddle it, since the split between the two is exactly what the plot is there to show.\n\nx is the estimates, y their labels, and ε the half-width of each interval — so the bar runs from x - ε to x + ε.\n\nx = [ 1.8, -2.4,  0.3,  2.9, -0.6,  1.1, -3.2,  0.9]   # the estimates\ny = [\"gene1\", \"gene2\", \"gene3\", \"gene4\",\n     \"gene5\", \"gene6\", \"gene7\", \"gene8\"]              # the labels\nε = [ 0.6,  0.8,  1.0,  0.7,  0.9,  1.4,  0.7,  0.5]   # the half-widths\n\n# which intervals clear zero — the ones the plot will pick out\n[(y[i], (x[i]-ε[i]) * (x[i]+ε[i]) > 0) for i in eachindex(x)]","title":"An example"},{"category":"section","location":"confidence/#The-default-plot","page":"Confidence","text":"Given the three vectors, we get one point per variable with its error bar, a red line at zero, and the points coloured by whether their interval clears the line. The variables run up the y axis in the order given.\n\nplot_confidence(x, y, ε)\n\n","title":"The default plot"},{"category":"section","location":"confidence/#Modifying-the-plot","page":"Confidence","text":"The recipe sets the significant colouring, the marker shape, and the zero line for itself, and forces the ones that make the plot legible — the legend off, the y axis stripped back. The rest is left as a default that yields to what we pass, so titles, labels, the marker, the canvas, and the standard Plots vocabulary are all available on top.","title":"Modifying the plot"},{"category":"section","location":"confidence/#Naming-the-axes-and-ordering-the-effects","page":"Confidence","text":"The axis labels are ours to set, and the figure reads best when the effects are sorted, so the largest and the smallest sit at the two ends rather than scattered. We sort the three vectors together and label the axes.\n\nord = sortperm(x)\n\nplot_confidence(x[ord], y[ord], ε[ord];\n                xlabel = \"effect size\",\n                ylabel = \"\",\n                title  = \"effects, sorted\")","title":"Naming the axes and ordering the effects"},{"category":"section","location":"confidence/#Styling","page":"Confidence","text":"Everything the recipe sets as a default yields to what we pass, so the marker, the canvas size, and the fonts are ours to change. Here we enlarge the markers, widen the canvas, and give the figure a title.\n\nplot_confidence(x[ord], y[ord], ε[ord];\n                color  = :Reds_3,\n                marker = 8,\n                xlabel = \"effect size\",\n                title  = \"styled\")","title":"Styling"},{"category":"section","location":"predicted_observations/#Predicted-versus-observed-plot","page":"Predict Observations","text":"This plot scatters the predicted value of each observation against its observed value, with the line of perfect prediction at forty five degrees — so how far a point sits from the line is how far the prediction missed. Points above the line were over-predicted, points below under-predicted, and the R squared in the corner gives the fraction of the variance accounted for.\n\nplot_predict_observations(observed, predicted; resp = 1, kwargs...)\n\nThe kernel PLS regression is the model this serves, through plskern_predict:\n\nplot_predict_observations(Y,     plskern_predict(m, X))         # the training fit\nplot_predict_observations(Ytest, plskern_predict(m, Xtest))     # a test set\n\nThe discriminant models predict a class rather than a value, so their fit is read from a confusion of the classes instead, and the decompositions predict nothing at all.\n\nThe frame is kept square with matched limits, so the diagonal really is at forty five degrees and the eye can judge the scatter against it — that is the whole point of the figure.","title":"Predicted versus observed plot"},{"category":"section","location":"predicted_observations/#Setup","page":"Predict Observations","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs\n","title":"Setup"},{"category":"section","location":"predicted_observations/#A-simulated-example","page":"Predict Observations","text":"A response built from the predictors plus noise, so the model has a real signal to fit and the points fall near — but not on — the line. Three responses, since a PLS regression can predict several at once and the plot draws one at a time.\n\nWe split the observations in two. The training half sets the coefficients, and the test half is held back, which is what lets us compare an honest fit against a flattering one further down.\n\nrng = StableRNG(20240801)\n\nn = 120    # observations\np = 15     # predictors\nq = 3      # responses\n\nXall = randn(rng, n, p)\nB    = randn(rng, p, q)\nYall = Xall * B .+ 0.5 .* randn(rng, n, q)\n\n# hold half the observations back\ntr = 1:60\nte = 61:120\n\nXtr, Ytr = Xall[tr, :], Yall[tr, :]\nXte, Yte = Xall[te, :], Yall[te, :]\n\nm = plskern(Xtr, Ytr; nlv = 5)\n\nsize(Ytr), size(Yte)","title":"A simulated example"},{"category":"section","location":"predicted_observations/#The-default-plot","page":"Predict Observations","text":"Given the observed and predicted matrices, we get the first response, the diagonal, and the R squared. This is the training fit — the same observations that set the coefficients, now predicted by them — so the points hug the line closely.\n\nplot_predict_observations(Ytr, plskern_predict(m, Xtr))\n\n","title":"The default plot"},{"category":"section","location":"predicted_observations/#Modifying-the-plot","page":"Predict Observations","text":"Only one argument changes what we draw: resp picks the response column, since a regression of several responses is drawn one at a time. Everything else is styling — pointcolor and linecolor_ for the two layers, refline and showr2 to remove either of them, and the standard attributes on top.\n\nThe trailing underscore on linecolor_ is deliberate. linecolor is a standard Plots attribute, so the bare name would be intercepted before it reached us — the same reason the scree plot spells it that way.","title":"Modifying the plot"},{"category":"section","location":"predicted_observations/#The-honest-fit","page":"Predict Observations","text":"The same model against the held-back half. The scatter is wider and the R squared lower, because these observations had no hand in setting the coefficients — this is the error the model would make on data it has not seen, and it is the version worth reporting.\n\nIf a test plot ever looks as tight as its training plot, something has leaked between the two halves.\n\nplot_predict_observations(Yte, plskern_predict(m, Xte);\n                          xlabel = \"observed\",\n                          ylabel = \"predicted\",\n                          title  = \"test set, response 1\")","title":"The honest fit"},{"category":"section","location":"predicted_observations/#A-different-response","page":"Predict Observations","text":"resp draws another column of the response matrix. The three responses were built with independent coefficients, so each is fitted with its own accuracy and the R squared differs between them — worth checking each rather than reporting the first and assuming the rest follow.\n\nplot_predict_observations(Yte, plskern_predict(m, Xte);\n                          resp   = 2,\n                          xlabel = \"observed\",\n                          ylabel = \"predicted\",\n                          title  = \"test set, response 2\")","title":"A different response"},{"category":"section","location":"predicted_observations/#Colors,-and-stripping-it-back","page":"Predict Observations","text":"pointcolor sets the scatter and linecolor_ the diagonal. Both refline = false and showr2 = false remove their layer entirely — worth doing when the figure sits in a panel whose caption already carries the R squared, or when several fits are being compared and only the scatter should differ.\n\nThe square frame is a default like any other, so aspect_ratio = :none releases it if the response has a range that makes a square frame wasteful.\n\nplot_predict_observations(Yte, plskern_predict(m, Xte);\n                          resp       = 3,\n                          pointcolor = \"#d94801\",\n                          linecolor_ = :black,\n                          showr2     = false,\n                          xlabel     = \"observed\",\n                          ylabel     = \"predicted\",\n                          title      = \"response 3, no R squared\",\n                          markersize = 5,\n                          size       = (550, 550))","title":"Colors, and stripping it back"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Public-functions-and-types","page":"API Reference","text":"These are the functions and types exported by BigRiverPlots.jl.","title":"Public functions and types"},{"category":"section","location":"api/#Internal-functions","page":"API Reference","text":"Internal helpers, documented for reference. These are not part of the public API and may change.","title":"Internal functions"},{"category":"method","location":"api/#BigRiverPlots.check_comps-Tuple{Int64, Int64}","page":"API Reference","text":"check_comps(comp::Int, ncomp::Int) =>\n\nChecks that a requested component index exists in a fitted model.\n\nArguments\n\ncomp is the index of the single component to be drawn.\nncomp is the number of components available in the fitted model.\n\nOutput\n\nNothing is returned. An error is thrown when the index falls outside 1:ncomp, so that an out of range request fails with a clear message instead of a bounds error raised deep inside Plots. This is the method the plots of a single component need, such as the loadings plot.\n\n\n\n\n\n","title":"BigRiverPlots.check_comps"},{"category":"method","location":"api/#BigRiverPlots.check_comps-Tuple{Tuple{Int64, Int64}, Int64}","page":"API Reference","text":"check_comps(comps::Tuple{Int, Int}, ncomp::Int) =>\n\nChecks that a requested pair of component indices exists in a fitted model.\n\nArguments\n\ncomps is a tuple naming the two components to be placed on the x and y axes.\nncomp is the number of components available in the fitted model.\n\nOutput\n\nNothing is returned. An error is thrown when either index falls outside 1:ncomp, so that an out of range request fails with a clear message instead of a bounds error raised deep inside Plots. This is the method the plots of two components need, such as the scores plot.\n\n\n\n\n\n","title":"BigRiverPlots.check_comps"},{"category":"method","location":"api/#BigRiverPlots.get_biplot_coords-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"getbiplotcoords(scores::Matrix{Float64}, loadings::Matrix{Float64}; \t\t\t\t  comps::Tuple{Int, Int} = (1, 2), varnames::Vector{String} = String[], \t\t\t\t  nonzero::Bool = false, ntop::Int = 0, arrowscale::Float64 = 0.0) =>\n\nReturns the coordinates of two components of a matrix of scores together with the loading arrows of the same two components, ready for plotting.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns), which places the points.\nloadings is the matrix of loadings, variables (rows) by components (columns), which points the arrows. It should come from the same fitted model as scores.\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\nvarnames is a vector of names, one per variable, default is String[] for no names, in which case the variables are named by their index. They are subset along with the arrows, which is why they are given here.\nnonzero keeps only the variables whose loading is not zero on at least one of the two components drawn, default is false. This is what the models with an L1 penalty need, since most of their arrows would otherwise be drawn as stubs of length zero piled at the origin.\nntop keeps only the ntop variables of largest arrow length, default is 0 for all of them. This is what the dense models need, since an arrow per variable over thousands of them buries the points.\narrowscale is the factor the arrows are scaled by, default is 0.0 to scale them automatically. The loadings are unit length, so on the scale of the scores the arrows would be invisibly short: they are stretched to about four fifths of the point cloud. The scaling is conventional, so the DIRECTIONS and the RELATIVE lengths of the arrows carry meaning while the absolute length does not.\n\nOutput\n\nsxy matrix contains the scores of the observations on the two components, in two columns.\naxy matrix contains the scaled arrow tips of the variables kept, in two columns.\nnames vector contains the names of the variables kept, one per row of axy.\n\n\n\n\n\n","title":"BigRiverPlots.get_biplot_coords"},{"category":"method","location":"api/#BigRiverPlots.get_ellipse_coords-Tuple{AbstractVector, AbstractVector}","page":"API Reference","text":"getellipsecoords(x::AbstractVector, y::AbstractVector; nstd::Float64 = 2.5, \t\t\t\t   npoints::Int = 100) =>\n\nReturns the points of a confidence ellipse around a cloud of observations.\n\nArguments\n\nx is a vector of the coordinates of the observations on the first component.\ny is a vector of the coordinates of the observations on the second component.\nnstd is the number of standard deviations the ellipse is drawn at, default is 2.5. A larger value draws a wider ellipse: at 2.5 the ellipse covers most of a class that is roughly normal, so a point outside it is worth a look.\nnpoints is the number of points the ellipse is traced with, default is 100.\n\nOutput\n\nex vector contains the coordinates of the ellipse on the first component.\ney vector contains the coordinates of the ellipse on the second component.\n\nThe ellipse is the unit circle scaled by nstd standard deviations along each of the principal axes of the covariance of the cloud, then rotated onto those axes and moved onto its center. It is computed here rather than taken from covellipse of StatsPlots, so that the package needs no dependency beyond RecipesBase and the standard library.\n\n\n\n\n\n","title":"BigRiverPlots.get_ellipse_coords"},{"category":"method","location":"api/#BigRiverPlots.get_jive_variance_coords-Tuple{Vector{Matrix{Float64}}, Vector{Matrix{Float64}}, Vector{Matrix{Float64}}}","page":"API Reference","text":"getjivevariance_coords(blocks::Vector{Matrix{Float64}}, joint::Vector{Matrix{Float64}}, \t\t\t\t\t\t individual::Vector{Matrix{Float64}}; \t\t\t\t\t\t blocknames::Vector{String} = String[]) =>\n\nReturns the fraction of each data block's variation that is joint, individual, and residual, ready for plotting as stacked bars.\n\nArguments\n\nblocks is the vector of raw data blocks the JIVE model was fitted on, one matrix per block, variables (rows) by observations (columns). They are needed because the fractions are measured against the total variation of each block.\njoint is the vector of joint structure matrices of the fitted model, m.J, one per block and the same shape as the block.\nindividual is the vector of individual structure matrices of the fitted model, m.A, one per block and the same shape as the block.\nblocknames is a vector of names, one per block, default is String[] for no names, in which case the blocks are named by their index.\n\nOutput\n\nvarJ vector contains the fraction of each block explained by the joint structure.\nvarI vector contains the fraction of each block explained by its individual structure.\nvarR vector contains the residual fraction, what neither the joint nor the individual structure accounts for, so the three sum to one for each block.\nnames vector contains the names of the blocks.\n\nThe fractions are the squared Frobenius norm of a structure over the squared norm of the block, following the definition of variation explained in JIVE. The residual is one minus the joint and the individual, so a block whose joint fraction is large is one that shares much of its variation with the other blocks.\n\n\n\n\n\n","title":"BigRiverPlots.get_jive_variance_coords"},{"category":"method","location":"api/#BigRiverPlots.get_levels-Tuple{AbstractVector}","page":"API Reference","text":"get_levels(group::AbstractVector) =>\n\nReturns the unique levels of a grouping vector, in order of first appearance.\n\nArguments\n\ngroup is a vector of class labels, one per observation, or an empty vector when the observations are not grouped.\n\nOutput\n\nvLevels vector contains the unique labels found in group, in order of first appearance, or an empty vector when group is empty. The recipes draw one series per level, which is what gives categorical colors and one legend entry per class.\n\n\n\n\n\n","title":"BigRiverPlots.get_levels"},{"category":"method","location":"api/#BigRiverPlots.get_loadings_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getloadingscoords(loadings::Matrix{Float64}; comp::Int = 1, \t\t\t\t\tvarnames::Vector{String} = String[], \t\t\t\t\tnonzero::Bool = false, ntop::Int = 0) =>\n\nReturns the loadings of one component of a matrix of loadings, ready for plotting, together with the names of the variables they belong to.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns). Any set of variable weights can be given: the loadings of a PCA or a sparse PCA, the right factors of a penalized matrix decomposition, the X loadings of a PLS regression, the loadings of a discriminant model, the canonical directions of a CCA, or the joint loadings of a block of a JIVE model.\ncomp is the component whose loadings are drawn, default is 1.\nvarnames is a vector of names, one per variable, default is String[] for no names, in which case the variables are named by their index. They are subset along with the loadings, which is why they are given here rather than passed to the plot as xticks: the caller cannot know which variables nonzero and ntop will keep.\nnonzero keeps only the variables whose loading is not zero, default is false. This is what the models with an L1 penalty need, since most of their loadings sit exactly at zero and drawing them all leaves the selected ones lost in a flat line.\nntop keeps only the ntop variables of largest absolute loading, in variable order, default is 0 for all of them. This is what the dense models need, since a loading over thousands of variables is not readable. When nonzero is also given, the zeros are dropped first and the largest of what remains are kept.\n\nOutput\n\nx vector contains the positions of the variables kept, one to the number kept.\ny vector contains the loadings of the variables kept on the component.\nnames vector contains the names of the variables kept, or their indices as strings when no varnames were given.\n\n\n\n\n\n","title":"BigRiverPlots.get_loadings_coords"},{"category":"method","location":"api/#BigRiverPlots.get_loadings_heatmap_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getloadingsheatmap_coords(loadings::Matrix{Float64}; comps::Vector{Int} = Int[], \t\t\t\t\t\t\tvarnames::Vector{String} = String[], \t\t\t\t\t\t\tcompnames::Vector{String} = String[], \t\t\t\t\t\t\tnonzero::Bool = false, ntop::Int = 0) =>\n\nReturns the loadings of a fitted model, ready for plotting as a heatmap, together with the names of the variables and of the components they belong to.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns). Any set of variable weights can be given: the loadings of a PCA or a sparse PCA, the right factors of a penalized matrix decomposition, the X loadings of a PLS regression, the loadings of a discriminant model, the canonical directions of a CCA, or the joint loadings of a block of a JIVE model.\ncomps is a vector naming the components to be drawn, default is Int[] for all of them. Unlike the loadings plot, which draws one component, the heatmap draws them together, which is what shows whether two components select the same variables.\nvarnames is a vector of names, one per variable, default is String[] for no names, in which case the variables are named by their index. They are subset along with the loadings, which is why they are given here rather than passed to the plot as yticks: the caller cannot know which variables nonzero and ntop will keep.\ncompnames is a vector of names, one per component, default is String[] for no names, in which case the components are named by their index. They are subset along with the loadings too, following comps.\nnonzero keeps only the variables whose loading is not zero on AT LEAST ONE of the components drawn, default is false. This is what the models with an L1 penalty need, since most of their rows are zero throughout and leave the heatmap a flat field of the midtone.\nntop keeps only the ntop variables of largest absolute loading, taken over the components drawn and returned in variable order, default is 0 for all of them. This is what the dense models need, since a heatmap of thousands of rows resolves to nothing. When nonzero is also given, the zeros are dropped first and the largest of what remains are kept.\n\nOutput\n\nz matrix contains the loadings kept, variables (rows) by components (columns).\nxnames vector contains the names of the components drawn.\nynames vector contains the names of the variables kept.\n\n\n\n\n\n","title":"BigRiverPlots.get_loadings_heatmap_coords"},{"category":"method","location":"api/#BigRiverPlots.get_mosaic_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getmosaiccoords(counts::Matrix{Float64}; rownames::Vector{String} = String[],                   colnames::Vector{String} = String[], xpad::Float64 = 0.02,                   ypad::Float64 = 0.02) Splits a contingency table into the tile geometry of a mosaic plot.\n\nArguments\n\ncounts is the contingency table, one row per row category and one column per column category, holding counts or any non negative weights.\nrownames is a vector of names, one per row, default is String[] in which case the rows are named by their index.\ncolnames is a vector of names, one per column, default is String[] in which case the columns are named by their index.\nxpad is the gap left between the columns, default is 0.02.\nypad is the gap left between the stacked tiles within a column, default is 0.02.\n\nOutput\n\nx, w are the centre and width of each column.\nbottoms, heights are the lower edge and height of every tile.\ncounts is passed back through, so the recipe can label the tiles with the raw count.\ncondprobs is the conditional probability of each cell within its column, P(y|x).\njointprobs is each cell's count over the grand total, its share of the whole table.\ncolmarginals is the total of each column, and coltotalprob its share of the grand total, so the top strip can show either the column count or the marginal P(x).\nrnames, cnames, yticks name the tiles and place the row ticks.\n\n\n\n\n\n","title":"BigRiverPlots.get_mosaic_coords"},{"category":"method","location":"api/#BigRiverPlots.get_pairs_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getpairscoords(scores::Matrix{Float64}; comps::Vector{Int} = Int[], \t\t\t\t compnames::Vector{String} = String[]) =>\n\nReturns the components of a matrix of scores to be crossed against one another, ready for plotting, together with the names they belong to.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns). Any set of sample coordinates can be given: the projected data of a PCA or a sparse PCA, the left factors of a penalized matrix decomposition, the X scores of a PLS regression, the X variates of a discriminant model, the canonical variates of a CCA, or the joint scores of a JIVE model.\ncomps is a vector naming the components to be crossed, default is Int[] for all of them. The grid drawn is square in the number given, so it grows fast: four components make sixteen cells, and six make thirty six. Three or four is usually as much as reads.\ncompnames is a vector of names, one per component of scores, default is String[] for no names, in which case the components are named by their index. They are subset along with the scores, following comps, which is why they are given here rather than passed to the plot as ticks.\n\nOutput\n\nz matrix contains the scores of the components kept, observations (rows) by components (columns).\nnames vector contains the names of the components kept, one per column of z.\n\n\n\n\n\n","title":"BigRiverPlots.get_pairs_coords"},{"category":"method","location":"api/#BigRiverPlots.get_predict_observations_coords-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"getpredictobservations_coords(observed::Matrix{Float64}, predicted::Matrix{Float64}; \t\t\t\t\t\t\t\tresp::Int = 1) =>\n\nReturns the observed and predicted values of one response, ready for plotting one against the other, together with the reference line and the coefficient of determination.\n\nArguments\n\nobserved is the matrix of observed responses, observations (rows) by responses (columns): the Y a PLS regression was fitted or tested against.\npredicted is the matrix of predicted responses of the same shape, as returned by plskern_predict.\nresp is the response column drawn, default is 1. A PLS regression can predict several responses at once, so one is chosen; call the plot again with a different resp for each.\n\nOutput\n\nx vector contains the observed values of the response.\ny vector contains the predicted values of the response.\nline tuple contains the two endpoints of the line of perfect prediction, as (lo, hi), spanning the range of the values shown so the diagonal reaches corner to corner.\nr2 is the coefficient of determination between observed and predicted, the fraction of the variance of the observed accounted for, so a value near one is a close fit.\n\n\n\n\n\n","title":"BigRiverPlots.get_predict_observations_coords"},{"category":"method","location":"api/#BigRiverPlots.get_scores_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getscorescoords(scores::Matrix{Float64}; comps::Tuple{Int, Int} = (1, 2)) =>\n\nReturns the coordinates of two components of a matrix of scores, ready for plotting.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns). Any set of sample coordinates can be given: the projected data of a PCA or a sparse PCA, the left factors of a penalized matrix decomposition, the X scores of a PLS regression, the X variates of a discriminant model, the canonical variates of a CCA, or the joint scores of a JIVE model.\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\n\nOutput\n\nx vector contains the scores of the observations on the first component.\ny vector contains the scores of the observations on the second component.\n\n\n\n\n\n","title":"BigRiverPlots.get_scores_coords"},{"category":"method","location":"api/#BigRiverPlots.get_scree_coords-Tuple{AbstractVector}","page":"API Reference","text":"getscreecoords(values::AbstractVector; ncomp::Int = 0, \t\t\t\t compnames::Vector{String} = String[], cumulative::Bool = false) =>\n\nReturns the per component values of a fitted model, ready for plotting as a scree, together with the names of the components they belong to.\n\nArguments\n\nvalues is a vector of one value per component: the variances of a PCA or a sparse PCA, the weights d of a penalized matrix decomposition or a sparse CCA, or the canonical correlations of a CCA. Any per component quantity can be given, since the scree only asks that it fall off from the first component to the last.\nncomp is the number of leading components to keep, default is 0 for all of them. A scree is read for its elbow, which is near the front, so the long tail can be dropped without losing the shape.\ncompnames is a vector of names, one per component, default is String[] for no names, in which case the components are named by their index. They are subset along with the values, which is why they are given here rather than passed to the plot as ticks.\ncumulative returns the running total of the values rather than the values themselves, default is false. On the proportion of variance of a PCA this gives the rising curve read against a threshold, the companion to the falling bars.\n\nOutput\n\nx vector contains the positions of the components kept, one to the number kept.\ny vector contains the values of the components kept, cumulated when asked.\nnames vector contains the names of the components kept, or their indices as strings when no compnames were given.\n\n\n\n\n\n","title":"BigRiverPlots.get_scree_coords"},{"category":"method","location":"api/#BigRiverPlots.get_sparsity_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getsparsitycoords(loadings::Matrix{Float64}; comps::Vector{Int} = Int[], ncomp::Int = 0, \t\t\t\t\tcompnames::Vector{String} = String[], asfraction::Bool = false) =>\n\nReturns the count of selected variables per component of a loading matrix, ready for plotting, together with the names of the components they belong to.\n\nArguments\n\nloadings is a matrix of loadings, variables (rows) by components (columns), from a model with an L1 penalty: the loadings of a sparse PCA, the right factors of a penalized matrix decomposition, the loadings of a sparse discriminant model, or the canonical vectors of a sparse CCA. A variable is counted as selected on a component when its loading there is not zero.\ncomps is a vector naming the components to be drawn, default is Int[] for all of them. It selects an explicit, possibly non contiguous, set, such as [1, 3, 4].\nncomp is the number of leading components to keep, default is 0 for all of them. It is a shorthand for the leading ncomp components, and is ignored when comps is given.\ncompnames is a vector of names, one per component of loadings, default is String[] for no names, in which case the components are named by their index. They are subset along with the counts, following comps, which is why they are given here rather than passed to the plot as ticks.\nasfraction returns the fraction of the variables selected rather than the count, default is false. On a model with many variables the fraction reads more easily than a raw count, since it does not depend on knowing how many variables there are.\n\nOutput\n\nx vector contains the positions of the components kept, one to the number kept.\ny vector contains the number of variables selected on each component, or the fraction when asked.\nnames vector contains the names of the components kept, or their indices as strings when no compnames were given.\n\n\n\n\n\n","title":"BigRiverPlots.get_sparsity_coords"},{"category":"method","location":"api/#BigRiverPlots.get_vip_coords-Tuple{Matrix{Float64}}","page":"API Reference","text":"getvipcoords(vips::Matrix{Float64}; comp::Int = 0, varnames::Vector{String} = String[], \t\t\t   above::Bool = false, ntop::Int = 0) =>\n\nReturns the VIP scores of one component of a VIP matrix, sorted from the largest to the smallest, ready for plotting as horizontal bars, together with the names of the variables they belong to.\n\nArguments\n\nvips is the matrix of Variable Importance in Projection scores, variables (rows) by components (columns), as returned by vip on a fitted plsda or splsda. Column h is the VIP through the first h components together, so the last column is the overall importance and is the one usually read.\ncomp is the component whose column is drawn, default is 0 for the last, the overall VIP. A VIP is cumulative, so an earlier column reads the importance built up to that component alone.\nvarnames is a vector of names, one per variable, default is String[] for no names, in which case the variables are named by their index. They are subset and sorted along with the scores, which is why they are given here.\nabove keeps only the variables scoring above one, default is false. One is the usual threshold for an important variable, since the VIP scores are scaled so that their mean square is one, so a score above one is above average.\nntop keeps only the ntop variables of largest VIP, default is 0 for all of them. This is what a model over many variables needs when the names are wanted, though the plot hides the names past a few dozen and shows every bar as a curve regardless.\n\nThe scores are always returned sorted from the largest to the smallest. The plot lays them out as horizontal bars up the y-axis, so over many variables the sorted bars read as a smooth curve falling away from the threshold, and the count of variables clearing one is the height at which the curve crosses the line.\n\nOutput\n\nx vector contains the positions of the variables, one to the number kept, in sorted order.\ny vector contains the VIP scores, from the largest to the smallest.\nnames vector contains the names of the variables in the same sorted order, or their indices as strings when no varnames were given.\n\n\n\n\n\n","title":"BigRiverPlots.get_vip_coords"},{"category":"method","location":"api/#BigRiverPlots.plot_biplot!-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"plot_biplot!(scores::Matrix{Float64}, loadings::Matrix{Float64}; comps::Tuple{Int, Int} = (1, 2), \t\t\t group::AbstractVector = [], varnames::Vector{String} = String[], \t\t\t nonzero::Bool = false, ntop::Int = 0, arrowscale::Float64 = 0.0, kwargs...) Adds a biplot of a fitted model to the current plot.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns), which places the points.\nloadings is the matrix of loadings, variables (rows) by components (columns), which points the arrows. It should come from the same fitted model as scores.\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\ngroup is a vector of class labels, one per observation, used to color the points and to trace one confidence ellipse per class, default is [] for no grouping.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as annotations because they are subset along with the arrows.\nnonzero keeps only the variables whose loading is not zero on at least one of the two components drawn, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of longest arrow, default is 0 for all of them, which is what the dense models need over many variables.\narrowscale is the factor the arrows are scaled by, default is 0.0 to scale them onto the cloud of points automatically.\n\n\n\n\n\n","title":"BigRiverPlots.plot_biplot!"},{"category":"method","location":"api/#BigRiverPlots.plot_biplot-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"plot_biplot(scores::Matrix{Float64}, loadings::Matrix{Float64}; comps::Tuple{Int, Int} = (1, 2), \t\t\tgroup::AbstractVector = [], varnames::Vector{String} = String[], \t\t\tnonzero::Bool = false, ntop::Int = 0, arrowscale::Float64 = 0.0, kwargs...) Generates a biplot of a fitted model, drawing the observations as points and the variables as arrows.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns), which places the points.\nloadings is the matrix of loadings, variables (rows) by components (columns), which points the arrows. It should come from the same fitted model as scores.\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\ngroup is a vector of class labels, one per observation, used to color the points and to trace one confidence ellipse per class, default is [] for no grouping.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as annotations because they are subset along with the arrows.\nnonzero keeps only the variables whose loading is not zero on at least one of the two components drawn, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of longest arrow, default is 0 for all of them, which is what the dense models need over many variables.\narrowscale is the factor the arrows are scaled by, default is 0.0 to scale them onto the cloud of points automatically.\n\n\n\n\n\n","title":"BigRiverPlots.plot_biplot"},{"category":"method","location":"api/#BigRiverPlots.plot_confidence-Tuple{Vector{<:AbstractFloat}, Vector{<:String}, Vector}","page":"API Reference","text":"plot_confidence((x::Vector{<:AbstractFloat}, \ty ::Vector{<:String}, \tε ::Vector; \tkwargs...)\n\nGenerates a confidence plot.\n\nArguments\n\nx contains estimated values.\ny is a vector where each element correponds the labels of each estimates included in x.  \nε vector contains the variations relative to each x-value estimates in the confidence interval. \n\nIf the variation in not symmetrical, `ε` should be a 2-tuple of vector, where the first vector \ncorresponds to the left variation and the second to the right to the x-estimates.\n\n\n\n\n\n","title":"BigRiverPlots.plot_confidence"},{"category":"method","location":"api/#BigRiverPlots.plot_jive_variance!-Tuple{Vector{Matrix{Float64}}, Vector{Matrix{Float64}}, Vector{Matrix{Float64}}}","page":"API Reference","text":"plotjivevariance!(blocks::Vector{Matrix{Float64}}, joint::Vector{Matrix{Float64}}, \t\t\t\t\tindividual::Vector{Matrix{Float64}}; blocknames::Vector{String} = String[], kwargs...) Adds a stacked bar plot of the joint, individual, and residual variation of each block of a JIVE model to the current plot.\n\nArguments\n\nblocks is the vector of raw data blocks the model was fitted on, one matrix per block, variables (rows) by observations (columns).\njoint is the vector of joint structure matrices, m.J, one per block.\nindividual is the vector of individual structure matrices, m.A, one per block.\nblocknames is a vector of names, one per block, default is String[] in which case the blocks are named by their index.\n\n\n\n\n\n","title":"BigRiverPlots.plot_jive_variance!"},{"category":"method","location":"api/#BigRiverPlots.plot_jive_variance-Tuple{Vector{Matrix{Float64}}, Vector{Matrix{Float64}}, Vector{Matrix{Float64}}}","page":"API Reference","text":"plotjivevariance(blocks::Vector{Matrix{Float64}}, joint::Vector{Matrix{Float64}}, \t\t\t\t   individual::Vector{Matrix{Float64}}; blocknames::Vector{String} = String[], kwargs...) Generates a stacked bar plot of the joint, individual, and residual variation of each block of a JIVE model.\n\nArguments\n\nblocks is the vector of raw data blocks the model was fitted on, one matrix per block, variables (rows) by observations (columns).\njoint is the vector of joint structure matrices, m.J, one per block.\nindividual is the vector of individual structure matrices, m.A, one per block.\nblocknames is a vector of names, one per block, default is String[] in which case the blocks are named by their index.\n\n\n\n\n\n","title":"BigRiverPlots.plot_jive_variance"},{"category":"method","location":"api/#BigRiverPlots.plot_loadings!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_loadings!(loadings::Matrix{Float64}; comp::Int = 1, varnames::Vector{String} = String[], \t\t\t   nonzero::Bool = false, ntop::Int = 0, kwargs...) Adds a bar plot of the variable loadings of one component of a fitted model to the current plot.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns).\ncomp is the component whose loadings are drawn, default is 1.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as xticks because they are subset along with the loadings.\nnonzero keeps only the variables whose loading is not zero, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of largest absolute loading, default is 0 for all of them, which is what the dense models need over many variables.\n\n\n\n\n\n","title":"BigRiverPlots.plot_loadings!"},{"category":"method","location":"api/#BigRiverPlots.plot_loadings-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_loadings(loadings::Matrix{Float64}; comp::Int = 1, varnames::Vector{String} = String[], \t\t\t  nonzero::Bool = false, ntop::Int = 0, kwargs...) Generates a bar plot of the variable loadings of one component of a fitted model.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns).\ncomp is the component whose loadings are drawn, default is 1.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as xticks because they are subset along with the loadings.\nnonzero keeps only the variables whose loading is not zero, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of largest absolute loading, default is 0 for all of them, which is what the dense models need over many variables.\n\n\n\n\n\n","title":"BigRiverPlots.plot_loadings"},{"category":"method","location":"api/#BigRiverPlots.plot_loadings_heatmap!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plotloadingsheatmap!(loadings::Matrix{Float64}; comps::Vector{Int} = Int[], \t\t\t\t\t   varnames::Vector{String} = String[], compnames::Vector{String} = String[], \t\t\t\t\t   nonzero::Bool = false, ntop::Int = 0, kwargs...) Adds a heatmap of the variable loadings of every component of a fitted model to the current plot.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns).\ncomps is a vector naming the components to be drawn, default is Int[] for all of them.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as yticks because they are subset along with the loadings.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index.\nnonzero keeps only the variables whose loading is not zero on at least one of the components drawn, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of largest absolute loading over the components drawn, default is 0 for all of them, which is what the dense models need over many variables.\n\n\n\n\n\n","title":"BigRiverPlots.plot_loadings_heatmap!"},{"category":"method","location":"api/#BigRiverPlots.plot_loadings_heatmap-Tuple{Matrix{Float64}}","page":"API Reference","text":"plotloadingsheatmap(loadings::Matrix{Float64}; comps::Vector{Int} = Int[], \t\t\t\t\t  varnames::Vector{String} = String[], compnames::Vector{String} = String[], \t\t\t\t\t  nonzero::Bool = false, ntop::Int = 0, kwargs...) Generates a heatmap of the variable loadings of every component of a fitted model.\n\nArguments\n\nloadings is the matrix of loadings, variables (rows) by components (columns).\ncomps is a vector naming the components to be drawn, default is Int[] for all of them.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as yticks because they are subset along with the loadings.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index.\nnonzero keeps only the variables whose loading is not zero on at least one of the components drawn, default is false, which is what the models with an L1 penalty need.\nntop keeps only the ntop variables of largest absolute loading over the components drawn, default is 0 for all of them, which is what the dense models need over many variables.\n\n\n\n\n\n","title":"BigRiverPlots.plot_loadings_heatmap"},{"category":"method","location":"api/#BigRiverPlots.plot_mosaic!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_mosaic!(counts::Matrix{Float64}; rownames::Vector{String} = String[],              colnames::Vector{String} = String[], mode::Symbol = :count,              marginals::Symbol = :none, xpad::Float64 = 0.02, ypad::Float64 = 0.02,              kwargs...) Adds a mosaic plot of a contingency table to the current plot.\n\nArguments\n\ncounts is the contingency table, one row per row category and one column per column category, holding counts or any non negative weights.\nrownames is a vector of names, one per row, default is String[] in which case the rows are named by their index.\ncolnames is a vector of names, one per column, default is String[] in which case the columns are named by their index.\nmode is what each tile is labelled with, default is :count. :count writes the raw count, :conditional the conditional probability P(y|x), and :total the joint probability, the cell's share of the whole table.\nmarginals is what the strip along the top shows, default is :none. :count writes the total of each column and :probability the marginal probability of each column.\nxpad is the gap left between the columns, default is 0.02.\nypad is the gap left between the stacked tiles within a column, default is 0.02.\n\n\n\n\n\n","title":"BigRiverPlots.plot_mosaic!"},{"category":"method","location":"api/#BigRiverPlots.plot_mosaic-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_mosaic(counts::Matrix{Float64}; rownames::Vector{String} = String[],             colnames::Vector{String} = String[], mode::Symbol = :count,             marginals::Symbol = :none, xpad::Float64 = 0.02, ypad::Float64 = 0.02,             kwargs...) Generates a mosaic plot of a contingency table, the area of each tile proportional to its count.\n\nArguments\n\ncounts is the contingency table, one row per row category and one column per column category, holding counts or any non negative weights.\nrownames is a vector of names, one per row, default is String[] in which case the rows are named by their index. They are given here rather than as yticks because the plot places them against the tiles rather than at even spacing.\ncolnames is a vector of names, one per column, default is String[] in which case the columns are named by their index.\nmode is what each tile is labelled with, default is :count. :count writes the raw count, :conditional the conditional probability P(y|x), and :total the joint probability, the cell's share of the whole table.\nmarginals is what the strip along the top shows, default is :none. :count writes the total of each column and :probability the marginal probability of each column.\nxpad is the gap left between the columns, default is 0.02.\nypad is the gap left between the stacked tiles within a column, default is 0.02.\n\n\n\n\n\n","title":"BigRiverPlots.plot_mosaic"},{"category":"method","location":"api/#BigRiverPlots.plot_pairs!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_pairs!(scores::Matrix{Float64}; comps::Vector{Int} = Int[], group::AbstractVector = [], \t\t\tcompnames::Vector{String} = String[], kwargs...) Adds a scatter matrix of the sample scores of a fitted model to the current plot.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns).\ncomps is a vector naming the components to be crossed, default is Int[] for all of them. At least two are needed, and three or four is usually as much as reads.\ngroup is a vector of class labels, one per observation, used to color the points of the cells off the diagonal, default is [] for no grouping.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as labels because they are subset along with the scores, and because each names both a row and a column of the grid.\n\n\n\n\n\n","title":"BigRiverPlots.plot_pairs!"},{"category":"method","location":"api/#BigRiverPlots.plot_pairs-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_pairs(scores::Matrix{Float64}; comps::Vector{Int} = Int[], group::AbstractVector = [], \t\t   compnames::Vector{String} = String[], kwargs...) Generates a scatter matrix of the sample scores of a fitted model, crossing every component against every other.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns).\ncomps is a vector naming the components to be crossed, default is Int[] for all of them. At least two are needed, and three or four is usually as much as reads.\ngroup is a vector of class labels, one per observation, used to color the points of the cells off the diagonal, default is [] for no grouping.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as labels because they are subset along with the scores, and because each names both a row and a column of the grid.\n\n\n\n\n\n","title":"BigRiverPlots.plot_pairs"},{"category":"method","location":"api/#BigRiverPlots.plot_predict_observations!-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"plotpredictobservations!(observed::Matrix{Float64}, predicted::Matrix{Float64}; resp::Int = 1, kwargs...) Adds a predicted versus observed plot of a regression to the current plot.\n\nArguments\n\nobserved is the matrix of observed responses, observations (rows) by responses (columns).\npredicted is the matrix of predicted responses of the same shape, as returned by plskern_predict.\nresp is the response column drawn, default is 1. Call again with a different resp for each response of a multivariate regression.\n\n\n\n\n\n","title":"BigRiverPlots.plot_predict_observations!"},{"category":"method","location":"api/#BigRiverPlots.plot_predict_observations-Tuple{Matrix{Float64}, Matrix{Float64}}","page":"API Reference","text":"plotpredictobservations(observed::Matrix{Float64}, predicted::Matrix{Float64}; resp::Int = 1, kwargs...) Generates a predicted versus observed plot of a regression, with the line of perfect prediction and the R squared.\n\nArguments\n\nobserved is the matrix of observed responses, observations (rows) by responses (columns).\npredicted is the matrix of predicted responses of the same shape, as returned by plskern_predict.\nresp is the response column drawn, default is 1. Call again with a different resp for each response of a multivariate regression.\n\n\n\n\n\n","title":"BigRiverPlots.plot_predict_observations"},{"category":"method","location":"api/#BigRiverPlots.plot_scores!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_scores!(scores::Matrix{Float64}; comps::Tuple{Int, Int} = (1, 2), \t\t\t group::AbstractVector = [], kwargs...) Adds a scatter plot of the sample scores of a fitted model to the current plot.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns).\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\ngroup is a vector of class labels, one per observation, used to color the points, default is [] for no grouping.\n\n\n\n\n\n","title":"BigRiverPlots.plot_scores!"},{"category":"method","location":"api/#BigRiverPlots.plot_scores-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_scores(scores::Matrix{Float64}; comps::Tuple{Int, Int} = (1, 2), \t\t\tgroup::AbstractVector = [], kwargs...) Generates a scatter plot of the sample scores of a fitted model.\n\nArguments\n\nscores is the matrix of scores, observations (rows) by components (columns).\ncomps is a tuple naming the two components placed on the x and y axes, default is (1, 2).\ngroup is a vector of class labels, one per observation, used to color the points, default is [] for no grouping.\n\n\n\n\n\n","title":"BigRiverPlots.plot_scores"},{"category":"method","location":"api/#BigRiverPlots.plot_scree!-Tuple{AbstractVector}","page":"API Reference","text":"plot_scree!(values::AbstractVector; ncomp::Int = 0, compnames::Vector{String} = String[], \t\t\tcumulative::Bool = false, kwargs...) Adds a scree plot of the per component values of a fitted model to the current plot.\n\nArguments\n\nvalues is a vector of one value per component, such as m.variances, m.d or m.corrs.\nncomp is the number of leading components to keep, default is 0 for all of them.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as xticks because they are subset along with the values.\ncumulative draws the running total of the values rather than the values themselves, default is false, useful on the proportion of variance of a PCA.\n\n\n\n\n\n","title":"BigRiverPlots.plot_scree!"},{"category":"method","location":"api/#BigRiverPlots.plot_scree-Tuple{AbstractVector}","page":"API Reference","text":"plot_scree(values::AbstractVector; ncomp::Int = 0, compnames::Vector{String} = String[], \t\t   cumulative::Bool = false, kwargs...) Generates a scree plot of the per component values of a fitted model, as bars with a line riding their tops.\n\nArguments\n\nvalues is a vector of one value per component, such as m.variances, m.d or m.corrs.\nncomp is the number of leading components to keep, default is 0 for all of them.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as xticks because they are subset along with the values.\ncumulative draws the running total of the values rather than the values themselves, default is false, useful on the proportion of variance of a PCA.\n\n\n\n\n\n","title":"BigRiverPlots.plot_scree"},{"category":"method","location":"api/#BigRiverPlots.plot_sparsity!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_sparsity!(loadings::Matrix{Float64}; ncomp::Int = 0, compnames::Vector{String} = String[], \t\t\t   asfraction::Bool = false, kwargs...) Adds a bar plot of the number of variables each component of a penalized model keeps nonzero to the current plot.\n\nArguments\n\nloadings is a matrix of loadings, variables (rows) by components (columns), from a model with an L1 penalty.\nncomp is the number of leading components to keep, default is 0 for all of them.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as xticks because they are subset along with the counts.\nasfraction draws the fraction of the variables selected rather than the count, default is false.\n\n\n\n\n\n","title":"BigRiverPlots.plot_sparsity!"},{"category":"method","location":"api/#BigRiverPlots.plot_sparsity-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_sparsity(loadings::Matrix{Float64}; ncomp::Int = 0, compnames::Vector{String} = String[], \t\t\t  asfraction::Bool = false, kwargs...) Generates a bar plot of the number of variables each component of a penalized model keeps nonzero.\n\nArguments\n\nloadings is a matrix of loadings, variables (rows) by components (columns), from a model with an L1 penalty.\nncomp is the number of leading components to keep, default is 0 for all of them.\ncompnames is a vector of names, one per component, default is String[] in which case the components are named by their index. They are given here rather than as xticks because they are subset along with the counts.\nasfraction draws the fraction of the variables selected rather than the count, default is false.\n\n\n\n\n\n","title":"BigRiverPlots.plot_sparsity"},{"category":"method","location":"api/#BigRiverPlots.plot_vip!-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_vip!(vips::Matrix{Float64}; comp::Int = 0, varnames::Vector{String} = String[], \t\t  above::Bool = false, ntop::Int = 0, kwargs...) Adds a horizontal bar plot of the Variable Importance in Projection of a discriminant model to the current plot.\n\nArguments\n\nvips is the matrix of VIP scores, variables (rows) by components (columns), as returned by vip on a fitted plsda or splsda.\ncomp is the component whose column is drawn, default is 0 for the last, the overall VIP.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as yticks because they are sorted and subset along with the scores. The plot hides them past a few dozen, since there is no room for a tick per variable.\nabove keeps only the variables scoring above one, the usual threshold for an important variable, default is false.\nntop keeps only the ntop variables of largest VIP, default is 0 for all of them.\n\n\n\n\n\n","title":"BigRiverPlots.plot_vip!"},{"category":"method","location":"api/#BigRiverPlots.plot_vip-Tuple{Matrix{Float64}}","page":"API Reference","text":"plot_vip(vips::Matrix{Float64}; comp::Int = 0, varnames::Vector{String} = String[], \t\t above::Bool = false, ntop::Int = 0, kwargs...) Generates a horizontal bar plot of the Variable Importance in Projection of a discriminant model, sorted, with a threshold line at one.\n\nArguments\n\nvips is the matrix of VIP scores, variables (rows) by components (columns), as returned by vip on a fitted plsda or splsda.\ncomp is the component whose column is drawn, default is 0 for the last, the overall VIP.\nvarnames is a vector of names, one per variable, default is String[] in which case the variables are named by their index. They are given here rather than as yticks because they are sorted and subset along with the scores. The plot hides them past a few dozen, since there is no room for a tick per variable.\nabove keeps only the variables scoring above one, the usual threshold for an important variable, default is false.\nntop keeps only the ntop variables of largest VIP, default is 0 for all of them.\n\n\n\n\n\n","title":"BigRiverPlots.plot_vip"},{"category":"section","location":"scree/#Scree-plot","page":"Scree","text":"The scree plot draws one bar per component, with a line riding their tops. The fall from the first component to the last is what we read: where it flattens is the elbow, and the components before it are the ones worth keeping.\n\nplot_scree(values; ncomp = 0, compnames = ..., cumulative = false, kwargs...)\n\nIt takes a plain vector of one value per component, so it is not tied to any model. Each model passes whatever field measures the strength of its components:\n\nModel Call\npca plot_scree(m.variances)\nspc plot_scree(m.variances)\npmd plot_scree(m.d)\ncca plot_scree(m.corrs)\nscca plot_scree(m.cors)\njive plot_scree(vcat(m.r, m.ri))\n\nThe regression and discriminant models — plskern, plsda, splsda — store no per-component strength, so they have no scree to draw.","title":"Scree plot"},{"category":"section","location":"scree/#Setup","page":"Scree","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs","title":"Setup"},{"category":"section","location":"scree/#A-simulated-example","page":"Scree","text":"The same data as the other notebooks: three latent signals driving twenty variables. Because only three signals are at work, we expect the scree to fall sharply after the third component — that fall is exactly what the plot is for.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\nm = pca(X; k = 8)\n\nround.(m.variances, digits = 3)","title":"A simulated example"},{"category":"section","location":"scree/#The-default-plot","page":"Scree","text":"Given the variances, we get a bar per component with the line on top and the components ticked by index. The elbow after the third component should be plain.\n\nplot_scree(m.variances)\n\n","title":"The default plot"},{"category":"section","location":"scree/#Modifying-the-plot","page":"Scree","text":"There are two things to decide, and they are independent.\n\nThe first is what quantity we pass, which the plot never chooses for us:\n\nm.variances — the raw variance of each component, which falls off\nm.propOFvar — the same shape scaled to sum to one, so we can say \"PC1 explains 55%\"\n\nThe second is whether it is summed, which is what cumulative controls. Passing propOFvar with cumulative = true gives the rising curve we read against a threshold.\n\nOn top of that, ncomp truncates the tail, compnames names the components, and showline removes the line.","title":"Modifying the plot"},{"category":"section","location":"scree/#Naming-and-truncating","page":"Scree","text":"compnames names the components — passed as an argument rather than as xticks, because the names are subset along with the values, so a truncated plot keeps the right labels. ncomp keeps only the leading components, dropping a long flat tail that tells us nothing.\n\nHere we also switch from raw variance to the proportion, which makes the y axis directly interpretable.\n\nplot_scree(m.propOFvar;\n           compnames = [\"PC $(j)\" for j in 1:8],\n           ncomp     = 5,\n           ylabel    = \"proportion of variance\",\n           title     = \"the first five components\")","title":"Naming and truncating"},{"category":"section","location":"scree/#The-cumulative-view","page":"Scree","text":"With cumulative = true the running total is drawn instead of the per-component value. Read against the proportion of variance it answers the practical question directly: how many components do we need to reach 90 percent?\n\nWe drop the line here with showline = false. On a cumulative curve the bars rise to a plateau near one and the line rides that plateau, which reads oddly — bars alone are cleaner. We also draw a threshold across the plot to read against.\n\nplot_scree(m.propOFvar;\n           compnames  = [\"PC $(j)\" for j in 1:8],\n           cumulative = true,\n           showline   = false,\n           ylabel     = \"cumulative proportion\",\n           title      = \"cumulative variance explained\",\n           ylims      = (0, 1.05))\n\nhline!([0.9]; linestyle = :dash, color = :grey, label = \"90%\")","title":"The cumulative view"},{"category":"section","location":"scree/#Colors-and-the-rest","page":"Scree","text":"Two color knobs of our own: barcolor for the bars and linecolor_ for the line on top. The trailing underscore is deliberate — linecolor is a standard Plots attribute, so the bare name would be intercepted before it reached us.\n\nEverything else the plot sets for itself yields to what we pass, so the usual attributes stack on top.\n\nplot_scree(m.variances;\n           compnames     = [\"PC $(j)\" for j in 1:8],\n           barcolor      = \"#fdae6b\",\n           linecolor_    = \"#e6550d\",\n           ylabel        = \"variance\",\n           title         = \"PCA scree\",\n           size          = (700, 400),\n           guidefontsize = 10)","title":"Colors and the rest"},{"category":"section","location":"scree/#The-line-alone","page":"Scree","text":"Some readers prefer the scree as a plain curve, without the bars underneath. The elbow is what we came to find, and the bars mostly restate what the line already shows — so on a small figure, or beside several others, dropping them leaves a cleaner shape.\n\nPassing alpha will not do it: the bar color is set by the plot itself, so a global transparency either gets overridden or fades the line along with it. We make the bar color transparent instead, which leaves the line and the markers untouched.\n\nplot_scree(m.variances;\n           compnames = [\"PC $(j)\" for j in 1:8],\n           barcolor  = :transparent,\n           ylabel    = \"variance\",\n           title     = \"PCA scree, the line alone\")","title":"The line alone"},{"category":"section","location":"biplot/#Biplot","page":"Biplot","text":"The biplot overlays the two previous views. The observations are drawn as points, exactly as in the scores plot, and the variables are drawn as arrows from the origin, their direction and length taken from the loadings. Reading the two together tells us not just that the classes separate, but which variables drive the separation.\n\nplot_biplot(scores, loadings; group = ..., varnames = ..., ntop = ..., arrowscale = ..., kwargs...)\n\nIt takes a scores matrix (observations in rows) and a loadings matrix (variables in rows), so as with the other two it never sees the model itself.\n\nWe use the same simulation as the scores and loadings notebooks, so all three describe one fit.","title":"Biplot"},{"category":"section","location":"biplot/#Setup","page":"Biplot","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs\n\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\n# the class is read off the first latent signal\ny = [latent[i, 1] > 0.4 ? \"a\" : latent[i, 1] < -0.4 ? \"c\" : \"b\" for i in 1:n]\n\n# names for the arrows\nvnames = [\"gene$(i)\" for i in 1:p]\n\nm = pca(X; k = 5)\nS = pca_transform(m, X)\n\nsize(S), size(m.loadings)","title":"Setup"},{"category":"section","location":"biplot/#The-default-plot","page":"Biplot","text":"Given the scores and the loadings, we get the first two components: every observation in one series, and an arrow for every variable, named by index. With twenty variables the arrows crowd the canvas — thinning them is the first thing we usually do.\n\nplot_biplot(S, m.loadings)\n\n","title":"The default plot"},{"category":"section","location":"biplot/#Modifying-the-plot","page":"Biplot","text":"The arguments that change what we draw are the same family as before — group and comps act on the points, ntop and nonzero on the arrows, varnames names them. The rest are styling: the arrow color and scale, the crosshairs, the class ellipses, and the standard Plots vocabulary.","title":"Modifying the plot"},{"category":"section","location":"biplot/#Grouping,-naming,-and-thinning-the-arrows","page":"Biplot","text":"group colors the observations by class and traces an ellipse around each one. ntop keeps only the variables with the largest absolute loading, which is what makes the arrow layer readable — eight is usually plenty. varnames labels those arrows; without it they are named by their index.\n\nRead this figure as two layers at once: the classes separate left to right, and the arrows pointing that way name the variables responsible.\n\nplot_biplot(S, m.loadings;\n            group    = y,\n            varnames = vnames,\n            ntop     = 8,\n            xlabel   = \"PC 1\",\n            ylabel   = \"PC 2\",\n            title    = \"PCA biplot, eight strongest variables\")","title":"Grouping, naming, and thinning the arrows"},{"category":"section","location":"biplot/#Scaling-the-arrows","page":"Biplot","text":"The scores and the loadings live on quite different scales, so the arrows have to be stretched to share a canvas with the points. arrowscale sets that stretch. It carries no statistical meaning — it is chosen so the arrows reach into the cloud without swamping it, and it needs adjusting whenever the data or the number of components changes.\n\nToo small and the arrows huddle at the origin; too large and they run off the axes. Try both to see the range.\n\nplot_biplot(S, m.loadings;\n            group      = y,\n            varnames   = vnames,\n            ntop       = 8,\n            arrowscale = 15.0,\n            title      = \"arrowscale = 15.0\")","title":"Scaling the arrows"},{"category":"section","location":"biplot/#Colors,-ellipses,-and-the-rest","page":"Biplot","text":"arrowcolor sets the arrows, origincolor the crosshairs at zero. The class ellipses are drawn at nstd standard deviations and can be turned off with ellipse = false — worth doing when the classes overlap heavily and the rings obscure more than they show. A class of fewer than three observations gets no ellipse, since there is nothing to trace.\n\nEverything the plot sets for itself yields to what we pass, so the standard attributes stack on top. The legend sits outside the axes by default, because the arrows reach into every corner where a legend would otherwise sit.\n\nplot_biplot(S, m.loadings;\n            group       = y,\n            varnames    = vnames,\n            ntop        = 6,\n            arrowscale  = 10.0,\n            arrowcolor  = \"#d7191c\",\n            origincolor = :black,\n            nstd        = 2.0,\n            xlabel      = \"PC 1\",\n            ylabel      = \"PC 2\",\n            title       = \"restyled, two standard deviations\",\n            marker      = 4,\n            size        = (900, 550))","title":"Colors, ellipses, and the rest"},{"category":"section","location":"jive_variance/#JIVE-variance-plot","page":"JIVE Variance","text":"JIVE splits the variation of several data blocks into structure that is shared between them and structure that belongs to each alone. This plot makes that split visible: one bar per block, stacked into three parts — residual at the base, individual above it, joint on top — so how much of a block is shared, how much is its own, and how much neither structure catches all read at a glance.\n\nplot_jive_variance(blocks, joint, individual; blocknames = ..., kwargs...)\n\nIt serves JIVE alone, since JIVE is the model that separates joint from individual variation:\n\njive      plot_jive_variance(dat, m.J, m.A)\n\nThe blocks are passed alongside the fit because the fractions are measured against the total variation of each block, which the fitted model does not store. The single-table models have no blocks to compare, and the two-block correlation models separate no joint from individual variation, so no other model has this figure.\n\nThe three fractions sum to one, so every bar reaches the same height and what we read is how the whole of each block divides.","title":"JIVE variance plot"},{"category":"section","location":"jive_variance/#Setup","page":"JIVE Variance","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs\nusing Statistics\nusing LinearAlgebra\n","title":"Setup"},{"category":"section","location":"jive_variance/#A-simulated-example","page":"JIVE Variance","text":"Three blocks of different sizes, each built from a signal shared by all three plus a signal of its own plus noise — so every bar should carry a real joint part, a real individual part, and a modest residual.\n\nNote the layout: JIVE holds the variables in rows and the observations in columns, the transpose of what the single-table models expect. The three blocks therefore differ in their number of rows but share their number of columns.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations, shared by every block\n\n# the signal every block shares\njoint_signal = randn(rng, 3, n)\n\nXs = [\n    randn(rng, 10, 3) * joint_signal .+ 0.6 .* randn(rng, 10, n) .+ 0.3 .* randn(rng, 10, n),\n    randn(rng, 8,  3) * joint_signal .+ 0.6 .* randn(rng, 8,  n) .+ 0.3 .* randn(rng, 8,  n),\n    randn(rng, 12, 3) * joint_signal .+ 0.6 .* randn(rng, 12, n) .+ 0.3 .* randn(rng, 12, n),\n]\n\n[size(b) for b in Xs]   # variables by observations","title":"A simulated example"},{"category":"section","location":"jive_variance/#Scaling-the-blocks","page":"JIVE Variance","text":"This step is easy to skip and gives quietly wrong answers if we do.\n\nJIVE row-centers each block and then normalizes it, so that a block with more variables or a larger scale does not dominate the joint structure. The fitted J and A live on that scaled footing — so the fractions have to be measured against the scaled blocks, not the raw ones. Handing the raw blocks to the plot produces bars that look perfectly reasonable and are wrong.\n\nSo we scale first, fit on the scaled blocks, and pass those same scaled blocks to the plot.\n\nnel   = [size(X, 1) * size(X, 2) for X in Xs]\nsum_n = sum(nel)\n\n# row-center each block, then normalize so no block dominates\nDat = [ let Xi = X .- mean(X, dims = 2); Xi ./ (norm(Xi) * sqrt(sum_n)); end for X in Xs ]\n\nm = jive(Dat)\n\nprintln(\"joint rank: \", m.r, \"   individual ranks: \", m.ri)\n\n# the fractions the plot will draw, as numbers\nfor i in 1:length(Dat)\n    total = norm(Dat[i])^2\n    vj = norm(m.J[i])^2 / total\n    vi = norm(m.A[i])^2 / total\n    println(\"  block $(i): joint \", round(vj, digits = 3),\n            \"  individual \", round(vi, digits = 3),\n            \"  residual \", round(1 - vj - vi, digits = 3))\nend","title":"Scaling the blocks"},{"category":"section","location":"jive_variance/#The-default-plot","page":"JIVE Variance","text":"Given the scaled blocks and the fitted structure, we get one stacked bar per block, named by index, with the legend outside the axes — the bars fill the frame to the top, so there is nowhere inside for it to sit.\n\nThe numbers printed above are what the segments should measure.\n\nplot_jive_variance(Dat, m.J, m.A)\n\n","title":"The default plot"},{"category":"section","location":"jive_variance/#Modifying-the-plot","page":"JIVE Variance","text":"There is only one argument that changes what we draw — blocknames, which names the bars. Everything else is styling: one color knob per segment, and the standard attributes on top.","title":"Modifying the plot"},{"category":"section","location":"jive_variance/#Naming-the-blocks","page":"JIVE Variance","text":"On real data the blocks are different assays, and naming them is most of what makes the figure readable. One name per block, in the order the blocks were fitted.\n\nplot_jive_variance(Dat, m.J, m.A;\n                   blocknames = [\"genes\", \"proteins\", \"metabolites\"],\n                   title      = \"Variation explained\")","title":"Naming the blocks"},{"category":"section","location":"jive_variance/#The-three-colors","page":"JIVE Variance","text":"jointcolor, individualcolor and residualcolor set the segments. They default to three shades of grey, which prints well and keeps the emphasis on the proportions — but a single hue running dark to light does the same job in color and is easier to follow across many blocks.\n\nWhatever we choose, the ordering matters: the joint segment should be the most prominent, since it is the one JIVE exists to find.\n\nplot_jive_variance(Dat, m.J, m.A;\n                   blocknames      = [\"genes\", \"proteins\", \"metabolites\"],\n                   jointcolor      = \"#08519c\",\n                   individualcolor = \"#3182bd\",\n                   residualcolor   = \"#bdd7e7\",\n                   title           = \"a colored scheme\")","title":"The three colors"},{"category":"section","location":"jive_variance/#The-frame","page":"JIVE Variance","text":"Everything the plot sets for itself yields to what we pass, including the legend placement and the canvas. Moving the legend inside works when the joint segment leaves room at the top — worth checking against the actual fit rather than assuming, since a block that is mostly joint will run the bar right up under it.\n\nplot_jive_variance(Dat, m.J, m.A;\n                   blocknames    = [\"genes\", \"proteins\", \"metabolites\"],\n                   legend        = :topright,\n                   ylabel        = \"proportion of variation\",\n                   title         = \"JIVE\",\n                   size          = (550, 450),\n                   guidefontsize = 11)","title":"The frame"},{"category":"section","location":"mosaic/#Mosaic-plot","page":"Mosaic","text":"The mosaic plot draws a contingency table as tiles. The width of each column is the share of the whole table that column holds, the marginal P(x), and the height of each tile within a column is the conditional P(y|x) — so the area of a tile is the joint probability of its cell, and the marginal and the conditional are both read from the one picture.\n\nplot_mosaic(counts; rownames = ..., colnames = ..., mode = ..., marginals = ..., kwargs...)\n\nIt takes a plain contingency table, so it is not tied to any model: any cross tabulation of two categorical variables is drawn the same way, whatever its shape.","title":"Mosaic plot"},{"category":"section","location":"mosaic/#Setup","page":"Mosaic","text":"using BigRiverPlots\nusing Plots\n","title":"Setup"},{"category":"section","location":"mosaic/#An-example-table","page":"Mosaic","text":"Three row categories against four column categories. The columns differ in total, so the mosaic columns come out at noticeably different widths, and each column splits differently between the three rows — which is the structure the plot is there to show.\n\nWe print the table and the quantities the three modes will draw, to compare against the tiles.\n\ncounts = Float64[30 10  5  2\n                 12 40  8  3\n                  4  6 25 20]\n\nrn = [\"low\", \"mid\", \"high\"]\ncn = [\"A\", \"B\", \"C\", \"D\"]\n\ntotal = sum(counts)\n\nprintln(\"column marginals P(x): \", round.(vec(sum(counts, dims = 1)) ./ total, digits = 3))\nprintln(\"P(level | A)         : \", round.(counts[:, 1] ./ sum(counts[:, 1]), digits = 3))\nprintln(\"joint of the corner  : \", round(counts[1, 1] / total, digits = 3), \"  (low, A)\")","title":"An example table"},{"category":"section","location":"mosaic/#The-default-plot","page":"Mosaic","text":"With nothing but the table and its names, we get one column per group at a width set by its total, split into the three levels, each tile labelled with its raw count. This is mode = :count, the default.\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"counts\")\n\n","title":"The default plot"},{"category":"section","location":"mosaic/#Modifying-the-plot","page":"Mosaic","text":"The geometry never changes — the tiles sit where they sit whatever we ask for. Two independent choices change what is written: mode sets the number in each tile, and marginals sets the strip along the top. On top of that are the usual styling knobs and the standard Plots vocabulary.","title":"Modifying the plot"},{"category":"section","location":"mosaic/#The-three-modes","page":"Mosaic","text":"mode chooses what each tile is labelled with. All three share the same geometry, so switching between them relabels the tiles without moving them.\n\n:count — the raw count\n:conditional — P(y|x), the tile's share of its own column, so each column's labels sum to one\n:total — the joint probability, the tile's share of the whole table, so all twelve labels sum to one\n\nHere is the conditional view: each column now reads as how that group splits across the three levels.\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            mode = :conditional,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"conditional  P(level | group)\")\n\nAnd the joint view, each tile as its share of the whole table:\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            mode = :total,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"joint  P(level, group)\")","title":"The three modes"},{"category":"section","location":"mosaic/#The-marginal-strip","page":"Mosaic","text":"marginals writes one value per column along the top, independent of the mode. :count shows the column totals, :probability the marginal P(x). Leaving it :none draws nothing above the tiles.\n\nBelow, the conditional tiles carry P(y|x) while the strip carries P(x) — so the figure shows the marginal and the conditional at once, and their product is the joint area of each tile.\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            mode = :conditional, marginals = :probability,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"conditional, with P(x) on top\")","title":"The marginal strip"},{"category":"section","location":"mosaic/#The-gaps,-and-the-colours","page":"Mosaic","text":"xpad sets the gap between columns and ypad the gap between the stacked tiles, so the two directions are controlled separately. marginaloffset sets how far the top strip sits above the bars. One colour per row is passed with mosaiccolors, and tilelinecolor, labelcolor and labelsize style the tiles and their numbers.\n\nlabelmintile is the size below which a tile is left unlabelled, since a number will not fit — lower it to label smaller tiles, raise it to blank more of them.\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            mode = :total, marginals = :count,\n            mosaiccolors = [\"#08519c\", \"#6baed6\", \"#c6dbef\"],\n            tilelinecolor = :black,\n            labelcolor = :black,\n            labelsize = 12,\n            marginalcolor = \"#d94801\",\n            xpad = 0.02,\n            ypad = 0.025,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"restyled\")","title":"The gaps, and the colours"},{"category":"section","location":"mosaic/#Horizontal","page":"Mosaic","text":"Passing permute = (:x, :y) swaps the axes, turning the columns into horizontal bars. Because the top strip then rides along the right, marginaloffset is bumped to push the marginals clear of the bar ends.\n\nplot_mosaic(counts;\n            rownames = rn, colnames = cn,\n            mode = :total, marginals = :probability,\n            permute = (:x, :y),\n            marginaloffset = 0.10,\n            legend = :outertopright,\n            xlabel = \"Group\", ylabel = \"Level\",\n            title = \"joint, permuted to horizontal\")","title":"Horizontal"},{"category":"section","location":"mosaic/#Conditioning-the-other-way","page":"Mosaic","text":"A mosaic of the transposed table conditions the other way round. Here the columns become the levels, and the split within each is P(group | level) — the same data, read as which groups make up each level rather than which levels make up each group. This is often the more useful of the two directions, and no recipe change is needed: the geometry is simply recomputed from the transpose.\n\nplot_mosaic(permutedims(counts);\n            rownames = cn, colnames = rn,\n            mode = :conditional,\n            xlabel = \"Level\", ylabel = \"Group\",\n            title = \"transpose: P(group | level)\")","title":"Conditioning the other way"},{"category":"section","location":"#BigRiverPlots","page":"Home","text":"(Image: CI) (Image: codecov) (Image: Docs-stable) (Image: Docs-dev) (Image: Pkg Status) (Image: GNU Affero license)","title":"BigRiverPlots"},{"category":"section","location":"#Description","page":"Home","text":"BigRiverPlots.jl is a versatile plotting package built in the Julia  programming language. The package consists of specific plotting recipes,  designed to streamline data visualization and enhance the process of statistical analysis.\n\n(Image: BigRiverPlots example plots)\n\nThe package is built using RecipesBase.jl.  Its recipes remain lightweight and use Plots.jl as a compatible plotting backend. To render and display these recipes, users must also load the Plots.jl package.\n\nThe plotting recipes are not tied to a particular statistical model or Julia package.  They operate directly on common model outputs, such as scores, loadings, and explained variances.\n\nConsequently, the plots can be used with any statistical method or decomposition that produces the required inputs.","title":"Description"},{"category":"section","location":"#Available-Plots","page":"Home","text":"BigRiverPlots.jl currently provides the following visualizations:\n\nBiplot — displays observations and variable loadings on the same coordinate system.\nConfidence plot — displays uncertainty regions around observations or group means.\nJIVE variance plot — summarizes the joint, individual, and residual variation for each data block.\nLoadings heatmap — displays variable loadings across multiple components.\nLoadings plot — shows the contribution of each variable to a selected component.\nMosaic plot — represents a contingency table using tiles with areas proportional to cell frequencies.\nPairs plot — displays pairwise relationships among several components.\nPredicted versus observed plot — assesses the fit of a regression or prediction model.\nScores plot — displays observations in the space defined by two components.\nScree plot — shows the variance explained by each component.\nSparsity plot — shows the number of variables retained in each component.\nVIP plot — displays Variable Importance in Projection scores from a projection-based model.","title":"Available Plots"},{"category":"section","location":"#Installation","page":"Home","text":"The BigRiverPlots package can be installed by running:\n\nusing Pkg\nPkg.add(\"BigRiverPlots\")\n\nor from the Julia REPL, press ] to enter pkg mode, and execute:\n\nadd BigRiverPlots\n\nFor the most recent (development) version, use:\n\nusing Pkg\nPkg.add(url = \"https://github.com/senresearch/BigRiverPlots.jl\", rev=\"main\")","title":"Installation"},{"category":"section","location":"#Contributing","page":"Home","text":"We welcome contributions that improve documentation, performance, testing, and functionality.  Users can contribute by opening an issue or submitting a pull request.","title":"Contributing"},{"category":"section","location":"#Questions","page":"Home","text":"If you have questions about contributing or using BigRiverPlots package, please communicate with the authors via GitHub.","title":"Questions"},{"category":"section","location":"loadings_heatmap/#Loadings-heatmap","page":"Loadings Heatmap","text":"The per-component loadings plot shows one component at a time. The heatmap shows them all at once: variables down the rows, components across the columns, each cell colored by the loading it holds.\n\nplot_loadings_heatmap(loadings; comps = [...], varnames = ..., compnames = ..., nonzero = ..., ntop = ..., kwargs...)\n\nThat layout answers a question the single-component view cannot — whether components 1 and 3 pick out overlapping variables, and whether they agree on the sign. The color scale is diverging and centered at zero, so a positive loading reads warm, a negative one cool, and anything near zero settles into the midtone.\n\nIt takes the same loadings matrix as before, variables in rows and components in columns.","title":"Loadings heatmap"},{"category":"section","location":"loadings_heatmap/#Setup","page":"Loadings Heatmap","text":"using BigRiverEssence\nusing BigRiverPlots\nusing Plots\nusing StableRNGs","title":"Setup"},{"category":"section","location":"loadings_heatmap/#A-simulated-example","page":"Loadings Heatmap","text":"The same three latent signals driving twenty variables. We fit a dense PCA and, further down, a sparse one — the two read very differently on a heatmap, and the difference is the reason this plot exists.\n\nrng = StableRNG(20240801)\n\nn = 90    # observations\np = 20    # variables\n\nlatent = randn(rng, n, 3)\nX = latent * randn(rng, 3, p) .+ 0.3 .* randn(rng, n, p)\n\nvnames = [\"gene$(i)\" for i in 1:p]\n\nm = pca(X; k = 4)\n\nsize(m.loadings)   # variables by components","title":"A simulated example"},{"category":"section","location":"loadings_heatmap/#The-default-plot","page":"Loadings Heatmap","text":"Given nothing but the loadings, we get every variable against every component, both axes ticked by index, with a colorbar showing the scale. The scale is padded symmetrically about zero, so warm and cool are directly comparable.\n\nplot_loadings_heatmap(m.loadings)\n\n","title":"The default plot"},{"category":"section","location":"loadings_heatmap/#Modifying-the-plot","page":"Loadings Heatmap","text":"The arguments that change what we draw are close cousins of the ones on the loadings plot, with two differences worth flagging.\n\ncomps here is a vector of components, not a single one — that is the whole point of the heatmap, so we can compare several side by side. And ntop ranks each variable by the largest loading it reaches on any of the components drawn, so a variable that is negligible on the first component but dominant on the third still earns its row. The survivors are then put back into variable order, so the axis still reads the way the data does.","title":"Modifying the plot"},{"category":"section","location":"loadings_heatmap/#Naming-and-selecting","page":"Loadings Heatmap","text":"varnames names the rows and compnames the columns; both are subset along with the matrix, so the labels stay correct whatever we filter. ntop keeps only the variables that matter somewhere, which is what keeps the row axis legible on a real data set.\n\nHere we take the six strongest variables across the first three components.\n\nplot_loadings_heatmap(m.loadings;\n                      comps     = [1, 2, 3],\n                      varnames  = vnames,\n                      compnames = [\"PC $(j)\" for j in 1:4],\n                      ntop      = 6,\n                      title     = \"six strongest variables across PC 1-3\")","title":"Naming and selecting"},{"category":"section","location":"loadings_heatmap/#A-sparse-fit,-with-and-without-the-zeros","page":"Loadings Heatmap","text":"This is where the heatmap earns its keep. A model with an L1 penalty drops most variables on most components, and on the map those dropped cells all land on the midtone — so we can see at a glance which variables each component selected, and whether the components overlap.\n\nFirst the full picture, every variable kept, mostly blank:\n\nm_spc = spc(X; k = 4, c = sqrt(p) / 2)\n\nplot_loadings_heatmap(m_spc.loadings;\n                      varnames  = vnames,\n                      compnames = [\"SPC $(j)\" for j in 1:4],\n                      title     = \"sparse PCA, all variables\")\n\nNow with nonzero = true, which drops any variable the penalty zeroed out on every component drawn. A row of zeros carries nothing and only squeezes the rest of the map, so removing them makes the selected structure far easier to read.\n\nplot_loadings_heatmap(m_spc.loadings;\n                      varnames  = vnames,\n                      compnames = [\"SPC $(j)\" for j in 1:4],\n                      nonzero   = true,\n                      title     = \"sparse PCA, selected variables only\")","title":"A sparse fit, with and without the zeros"},{"category":"section","location":"loadings_heatmap/#Color-and-the-rest","page":"Loadings Heatmap","text":"heatmapcolor sets the scheme and defaults to :RdBu. Any diverging scheme works — :balance and :coolwarm are the usual alternatives — but a diverging one matters here, since the sign of a loading is meaningful and a sequential scheme would hide it.\n\nmaxnames is the cutoff past which the row labels are dropped for numeric ticks, because names stop being legible once there are too many to fit. It defaults to 40; lowering it forces the plot back to indices.\n\nEverything else the plot sets for itself yields to what we pass — including colorbar, worth turning off when the figure sits beside others sharing one scale.\n\nplot_loadings_heatmap(m.loadings;\n                      varnames      = vnames,\n                      compnames     = [\"PC $(j)\" for j in 1:4],\n                      heatmapcolor  = :balance,\n                      xlabel        = \"Principal component\",\n                      ylabel        = \"Gene\",\n                      title         = \"PCA loadings\",\n                      colorbar      = true,\n                      size          = (600, 700),\n                      left_margin        = (5,:mm),\n                      guidefontsize = 11)","title":"Color and the rest"}]
}
